// RUN: tir mc --march=arm64 --stage=isel %s | filecheck %s

module {
  func @constant() -> !i64 {
    %value = constant {value = 4} : !i64
    return %value
  }
  module_end
}

// CHECK: arm64.movz
// CHECK-NOT: constant

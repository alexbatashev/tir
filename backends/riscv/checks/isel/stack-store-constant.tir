// RUN: tir mc --march=rv64i --stage=isel %s | filecheck %s

module {
  func @f() -> !i32 {
    %0 = ptr.alloca : !ptr.p<!i32>
    %1 = constant {value = 0} : !i32
    ptr.store %1, %0
    %2 = ptr.load %0 : !i32
    return %2
  }
  module_end
}

// CHECK: riscv.sw

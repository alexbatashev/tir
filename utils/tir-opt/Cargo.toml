[package]
name = "tir-opt"
version = "0.1.0"
edition = "2024"

[dependencies]
tir = { path = "../../core" }
clap = { version = "4.6.0", features = ["derive"] }

[[bin]]
name = "tir-opt"
path = "src/main.rs"


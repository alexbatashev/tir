// RUN: tir interp %s | filecheck %s

module {
  %main = func.func @main() -> !i32 {
    %nan = fp.constant {bits = 2141266757} : !f32
    %negative = fp.neg %nan : !f32
    %bits = bitcast %negative : !i32
    func.return %bits
  }
  module_end
}

// CHECK: i32 4288750405

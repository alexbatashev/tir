// RUN: tir interp %s | filecheck %s

module {
  %main = func.func @main() -> !i32 {
    %value = fp.constant {bits = 1325400064} : !f32
    %integer = fp.to_ui %value : !i32
    func.return %integer
  }
  module_end
}

// CHECK: i32 2147483648

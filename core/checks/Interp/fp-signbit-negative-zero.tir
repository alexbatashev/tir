// RUN: tir interp %s | filecheck %s

module {
  %main = func.func @main() -> !i1 {
    %zero = fp.constant {bits = 0} : !f64
    %negative_zero = fp.neg %zero : !f64
    %sign = fp.signbit %negative_zero : !i1
    func.return %sign
  }
  module_end
}

// CHECK: i1 1

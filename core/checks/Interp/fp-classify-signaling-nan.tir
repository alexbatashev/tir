// RUN: tir interp %s | filecheck %s

module {
  %main = func.func @main() -> !i1 {
    %nan = fp.constant {bits = 2139095041} : !f32
    %is_signaling = fp.classify %nan {class = "signaling_nan"} : !i1
    func.return %is_signaling
  }
  module_end
}

// CHECK: i1 1

// RUN: tir opt --pass builtin.func(scf-to-cfg) %s | filecheck %s

module {
  func @loop(%0: !i1) {
    scf.while {
      scf.condition %0
    } do scope(%1) {
      scf.break %1
    }
    return
  }
  module_end
}

// CHECK-NOT: scf.
// CHECK: cond_br
// CHECK: br ^bb
// CHECK: return

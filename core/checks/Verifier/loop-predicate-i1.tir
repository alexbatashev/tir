// RUN: not tir opt --verify %s 2>&1 | filecheck %s

module {
  %fn_main = func.func @main(%0: !i32) -> !i32 {
    %1 = scf.loop (%2 = %0) {
      -> %2, %2, %2
    }
    func.return %1
  }
  module_end
}

// CHECK: scf.loop predicate must have type i1

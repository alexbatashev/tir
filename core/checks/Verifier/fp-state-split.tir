// RUN: not tir opt --verify %s 2>&1 | filecheck %s

module {
  %f = func.func @main() {
    %env = state.entry_state : !state<fp.env>
    %left, %right = state.split state(%env)
    func.return
  }
  module_end
}

// CHECK: state.split only partitions memory states of one type

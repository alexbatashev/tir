// RUN: not tir opt --verify %s 2>&1 | filecheck %s

module {
  %f = func.func @main() {
    %env = state.entry_state : !state<fp.env>
    func.return state(%env, %env)
  }
  module_end
}

// CHECK: state %{{[0-9]+}} has incompatible concurrent uses

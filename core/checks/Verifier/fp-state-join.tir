// RUN: not tir opt --verify %s 2>&1 | filecheck %s

module {
  %f = func.func @main() {
    %env = state.entry_state : !state<fp.env>
    %next = state.join state(%env)
    func.return
  }
  module_end
}

// CHECK: state.join only merges memory states of one type

// RUN: tir opt --verify %s | filecheck %s

module {
  %f = func.func @add(%a: !f64, %b: !f64) -> !f64 {
    %sum = fp.add %a, %b : !f64
    -> %sum
  }
  module_end
}

// CHECK: %{{[0-9]+}} = fp.add %{{[0-9]+}}, %{{[0-9]+}} : !f64

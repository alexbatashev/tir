// RUN: tir opt %s | filecheck %s

module {
  %fn_f = func.func @f() {
    %0 = state.entry_state : !state<memory>
    func.return
  }
  module_end
}

// CHECK: %{{[0-9]+}} = func.func @f() {
// CHECK-NEXT: %{{[0-9]+}} = state.entry_state : !state<memory>
// CHECK-NEXT: func.return

// RUN: tir opt %s | filecheck %s

module {
  func @f() {
    %0 = entry_state : !state
    return
  }
  module_end
}

// CHECK: func @f() {
// CHECK-NEXT: %{{[0-9]+}} = entry_state : !state
// CHECK-NEXT: return
